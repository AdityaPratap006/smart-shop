import React, { useEffect} from 'react';

const ScrollThrough = ({children}) => {

    

    useEffect(()=>{
        // console.log(window.screen.availHeight);
        // console.log(element);
        
       
    },[]);

    return (
        <div  >
            {children}
        </div>
    )
}

export default ScrollThrough;
